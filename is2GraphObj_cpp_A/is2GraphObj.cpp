// Archivo DLL principal.
#include "stdafx.h"
#include "is2GraphObj.h"

namespace is2GraphObject {
	//----------------------------------------------------------------------
	// Constructor por defecto.
	/*PointType::PointType()
	{
		cX = 0.0;
		cY = 0.0;
		cZ = 0.0;
	}

	//----------------------------------------------------------------------
	// Constructort de Copia. 
	PointType::PointType (const PointType &P)
	{
		cX = P.cX;
		cY = P.cY;
		cZ = P.cZ;
	}

	//----------------------------------------------------------------------
	// Contructor que toma 2 parammetros, [X, Y].
	PointType::PointType (double x, double y)
	{
		cX = x;
		cY = y;
		cZ = 0.0;
	}

	//----------------------------------------------------------------------
	// Contructor que toma 3 parammetros, [X, Y, Z].
	PointType::PointType (double x, double y, double z)
	{
		cX = x;
		cY = y;
		cZ = z;
	}*/

	//=============================================================================================//
	// ClassName  : 
	//
	// Description: 
	//							
	//
	// Revision   : 
	//=============================================================================================//

	//------------------------------------------------------------------------//
	// Establece la precision (posiciones decimales) que tiene en cuenta is2Graph
	// para comprobar el valor del Cero Real.
	/*void is2GraphObj::SetPresicion (unsigned short d)
	{
	}*/
}